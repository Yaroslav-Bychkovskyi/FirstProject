spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/message_stats
    username: message_stats_user
    password: password

    hikari:
      maximum-pool-size: 5
      minimum-idle: 1

  jpa:
    hibernate:
      ddl-auto: update


    database-platform: org.hibernate.dialect.PostgreSQLDialect


